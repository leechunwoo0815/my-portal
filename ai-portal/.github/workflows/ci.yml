name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          cd ai-portal/backend
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: cd ai-portal/backend && python -m pytest tests/ -v --tb=short

  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ai-portal/frontend/package-lock.json

      - name: Install dependencies
        run: cd ai-portal/frontend && npm ci

      - name: Type check
        run: cd ai-portal/frontend && npx vue-tsc --noEmit

      - name: Build
        run: cd ai-portal/frontend && npm run build
